<?xml version="1.0" encoding="utf-8"?>
<!--
     SPDX-FileCopyrightText: The LineageOS Project
     SPDX-License-Identifier: Apache-2.0
-->
<resources>
    <!-- Doze: does this device support STATE_DOZE?  -->
    <bool name="doze_display_state_supported">true</bool>

    <!-- Type of a sensor that provides a low-power estimate of the desired display
         brightness, suitable to listen to while the device is asleep (e.g. during
         always-on display) -->
    <string name="doze_brightness_sensor_type" translatable="false">xiaomi.sensor.aod</string>

    <!-- Doze: Table that translates sensor values from the doze_brightness_sensor_type sensor
               to brightness values in the integer scale [1, 255]; -1 means keeping the current
               brightness. -->
    <integer-array name="config_doze_brightness_sensor_to_brightness">
        <item>-1</item>    <!-- 0.0: Never used -->
        <item>-1</item>    <!-- 1.0: Never used -->
        <item>-1</item>    <!-- 2.0: Never used -->
        <item>-1</item>    <!-- 3.0: Never used -->
        <item>24</item>    <!-- 4.0: Bright environment -->
        <item>4</item>     <!-- 5.0: Dark environment -->
    </integer-array>

    <!-- Doze: Table that translates sensor values from the doze_brightness_sensor_type sensor
               to an opacity value for a black scrim that is overlayed in AOD1.
               Valid range is from 0 (transparent) to 255 (opaque).
               -1 means keeping the current opacity. -->
    <integer-array name="config_doze_brightness_sensor_to_scrim_opacity">
        <item>-1</item>
        <item>0</item>
        <item>0</item>
        <item>0</item>
        <item>0</item>
        <item>0</item>
    </integer-array>

    <!-- Should we vibrate on an icon animation of the shelf. This should only be active if the
     vibrator is capable of subtle vibrations -->
    <bool name="config_vibrateOnIconAnimation">true</bool>

    <!-- Preferred refresh rate at keyguard, if supported by the display.
         120Hz keeps touch sampling high enough for UDFPS to work in AOD/screen-off state. -->
    <integer name="config_keyguardRefreshRate">120</integer>

    <!-- Preferred max refresh rate at keyguard, if supported by the display. -->
    <integer name="config_keyguardMaxRefreshRate">120</integer>

    <!-- AOD: disable clock on always-on display, show only UDFPS animation -->
    <bool name="config_dozeAlwaysOnDisplayAvailable">true</bool>
    <!-- Always-on kapalı: ekran 10sn sonra tamamen söner, sensörle pulse açılır -->
    <bool name="config_dozeAlwaysOnEnabled">false</bool>

    <!-- Hide clock/date/notifications on AOD — only UDFPS affordance shown -->
    <bool name="config_doze_show_clock">false</bool>
    <bool name="config_doze_show_date">false</bool>
    <bool name="config_doze_show_notifications">false</bool>
    <bool name="config_doze_show_charging_animation">true</bool>

    <!-- Show UDFPS affordance (fingerprint icon/animation) on AOD -->
    <bool name="config_show_udfps_affordance_on_aod">true</bool>

    <!-- Doze pulse: ekran kapandıktan sonra kaç ms AOD gösterilsin (10 saniye) -->
    <integer name="doze_pulse_duration_in">900</integer>
    <integer name="doze_pulse_duration_visible">10000</integer>
    <integer name="doze_pulse_duration_out">900</integer>

    <!-- Pulse tetikleyiciler: pickup, tap, tilt sensörleri -->
    <bool name="doze_pulse_on_pick_up">true</bool>
    <bool name="doze_pulse_on_double_tap">true</bool>
    <bool name="doze_pulse_on_long_press">false</bool>

    <!-- Pulse sonrası ekran tamamen kapansın (STATE_DOZE_SUSPEND) -->
    <bool name="doze_display_state_supported">true</bool>
    <bool name="doze_suspend_display_state_supported">true</bool>
</resources>
